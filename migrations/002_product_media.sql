-- Значок для обложки товара в витрине.
--
-- Отдельная миграция, а не правка первой: первая уже применена
-- на работающих базах, и её изменение развело бы схемы между стендами.
-- Это главное правило миграций — применённые не правят.

ALTER TABLE products ADD COLUMN IF NOT EXISTS emoji TEXT NOT NULL DEFAULT '📦';
