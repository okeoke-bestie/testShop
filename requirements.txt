# Зависимости боевого запуска. Версии закреплены снизу и сверху:
# без верхней границы очередная мажорная версия приезжает в сборку
# сама и ломает деплой в момент, когда его никто не трогал.
#
# Для разработки ставится не этот файл, а пакет с дополнениями:
#     pip install -e ".[all]"

fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.6,<3.0
psycopg[binary,pool]>=3.1,<4.0
